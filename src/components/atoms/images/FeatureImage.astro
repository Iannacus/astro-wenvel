---
const { img, alt } = Astro.props;
---

<img class="image rounded" src={img} alt={alt} />

<style>
  .image {
    position: absolute;
    left: -15%;
    z-index: 2;
    filter: drop-shadow(0px 35px 25px var(--blue-rgba-4));
    -webkit-text-fill-color: transparent;
    max-width: 55%;
  }

  @media (min-width: 990px;) {
    .image {
      left: -5%;
      max-width: 100%;
    }
  }
</style>
