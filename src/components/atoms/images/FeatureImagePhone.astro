---
const { img, alt } = Astro.props;
---

<img class="phone rounded" src={img} alt={alt} />

<style>
  .phone {
    position: relative;
    z-index: 2;
    filter: drop-shadow(0px 35px 25px var(--blue-rgba-4));
    max-width: 65%;
  }

  @media (min-width: 990px) {
    .phone {
      max-width: 100%;
    }
  }
</style>
